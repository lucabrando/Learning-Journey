class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

if val=0, then when no value is provided it defaults to 0
if next=None, then when no value is provided it defaults to empty
